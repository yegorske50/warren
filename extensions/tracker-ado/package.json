{
	"name": "@warren-ext/tracker-ado",
	"version": "0.0.0",
	"description": "Warren issue-tracker extension: warren-tracker/v1 over the Azure DevOps Boards REST API, out of process",
	"private": true,
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"start": "bun run src/index.ts",
		"test": "bun test",
		"typecheck": "tsc --noEmit -p tsconfig.json",
		"dev-server": "bun run src/dev-server.ts",
		"lint": "biome check src"
	},
	"license": "MIT",
	"engines": {
		"bun": ">=1.1.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.14",
		"@types/bun": "^1.2.0",
		"typescript": "^5.6.0"
	}
}
