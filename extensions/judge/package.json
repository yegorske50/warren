{
	"name": "@warren-ext/judge",
	"version": "0.0.0",
	"description": "Warren observer extension: a bounded, read-only LLM judge loop emitting rubric-v1 verdicts over run transcripts (agent-analytics §12)",
	"private": true,
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"start": "bun run src/index.ts",
		"test": "bun test",
		"typecheck": "tsc --noEmit -p tsconfig.json"
	},
	"license": "MIT",
	"engines": {
		"bun": ">=1.1.0"
	},
	"devDependencies": {
		"@types/bun": "^1.2.0",
		"typescript": "^5.6.0"
	},
	"dependencies": {
		"@earendil-works/pi-coding-agent": "^0.84.4",
		"@sinclair/typebox": "^0.34.52"
	}
}
