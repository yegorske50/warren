node_modules/
.bun/
dist/
src/ui/dist/
src/ui/node_modules/
src/ui/*.tsbuildinfo
data/
.env
.env.local
.env.deployed
*.log
.DS_Store
bun.lockb.bak
.run-plan-logs/
supabase/
# plot was removed from warren; the leftover local records stay untracked
.plot/

# warren seeds the rendered agent envelope here per run (warren-5585); the
# surrounding .warren/ config files (config.yaml, preview.yaml, triggers.yaml)
# stay tracked. Ignore the single seeded file, not the directory.
.warren/agent.json
.claude/settings.local.json

# pi agent session transcripts: leaked run logs (prompts, thinking, tool
# calls). Never commit — see sd warren-4c8d.
.pi/sessions/
.pi/tasks/
.idea/
.vscode/
test-results/
coverage/

# live GKE deployment values (real project ids / identity) — template: overlays/gke
deploy/k8s/overlays/gke-live/
deploy/k8s/overlays/gke-live-judge/
deploy/k8s/overlays/gke-live-campaign-controller/

# local design-sync working dirs + MCP config (may contain secrets)
.design-sync/
.ds-sync/
.mcp.json

# working design/planning docs that are not yet (or never will be) a seeds
# plan. Thinking-in-progress lives here so it has a home outside the tracker
# and outside git history; promote anything durable into docs or `sd plan`.
local-planning/
.atlas/
