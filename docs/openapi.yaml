# AUTO-GENERATED by `bun run gen:openapi` from `src/server/handlers/route-table.ts`.
# Do not edit by hand. CI fails if this file is out of sync.
openapi: 3.1.0
info:
  title: warren HTTP API
  version: 0.19.1
  description: >-
    Auto-generated from `src/server/handlers/route-table.ts`'s `ROUTE_TABLE`. Run `bun run
    gen:openapi` to refresh; CI fails if this schema drifts from the handler module. Response
    schemas for the core documents live under `components.schemas`, with enum vocabulary derived
    from `src/core/wire.ts`; request bodies and routes without a known envelope stay permissive —
    see docs/http-api.md for the canonical handler contracts.
tags:
  - name: agents
    description: '`/agents` routes.'
  - name: alerts
    description: '`/alerts` routes.'
  - name: analytics
    description: '`/analytics` routes.'
  - name: events
    description: '`/events` routes.'
  - name: extensions
    description: '`/extensions` routes.'
  - name: forge
    description: '`/forge` routes.'
  - name: github-app
    description: '`/github-app` routes.'
  - name: instance
    description: '`/instance` routes.'
  - name: meta
    description: Liveness, readiness, and version.
  - name: metrics
    description: '`/metrics` routes.'
  - name: ops
    description: '`/ops` routes.'
  - name: plan-runs
    description: '`/plan-runs` routes.'
  - name: preview
    description: '`/preview` routes.'
  - name: projects
    description: '`/projects` routes.'
  - name: runs
    description: '`/runs` routes.'
  - name: setup
    description: '`/setup` routes.'
  - name: whoami
    description: '`/whoami` routes.'
paths:
  /agents:
    get:
      operationId: listAgentsHandler
      tags:
        - agents
      summary: GET /agents
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  agents:
                    type: array
                    items:
                      $ref: '#/components/schemas/Agent'
                required:
                  - agents
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /agents/{name}:
    get:
      operationId: getAgentHandler
      tags:
        - agents
      summary: GET /agents/:name
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
  /alerts/heal:
    post:
      operationId: healAlertHandler
      tags:
        - alerts
      summary: POST /alerts/heal
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-3db0: closed-loop alert intake. Token-gated via the standard bearer gate (not
        auth-exempt); webhook senders carry the bearer.
  /analytics/behavior:
    get:
      operationId: listBehaviorAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/behavior
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /analytics/cost:
    get:
      operationId: listCostAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/cost
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /analytics/dispatch:
    get:
      operationId: listDispatchAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/dispatch
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /analytics/runs:
    get:
      operationId: listRunAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-97ae: spectators get the reduced projection — counts, rates and timings survive; USD
        aggregates stay redacted except the instance-wide cost/merged-PR ratio. The
        per-agent/per-model/ per-provider buckets keep their USD figures redacted because ratio ×
        merged count reconstructs spend.
  /events:
    get:
      operationId: listEventsHandler
      tags:
        - events
      summary: GET /events
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        pl-7e38 step 15 (warren-5eec): cross-run Event explorer query, same per-row `projectEvent`
        reduction.
  /events/stream:
    get:
      operationId: streamLifecycleEventsHandler
      tags:
        - events
      summary: GET /events/stream
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-f566: the global lifecycle notification stream (NDJSON, one `{runId, hook, state,
        ts}` line per lifecycle transition). The list pages hold ONE connection per tab and
        debounce-invalidate their list queries instead of polling /runs every 5s. `?follow=0` is a
        probe shorthand (immediate clean close, empty body). Operator-gated — a public spectator
        never gets a held-open feed of every run id on the instance (scenario 39); it stays on the
        fallback poll.
  /extensions/judge/verdicts.jsonl:
    get:
      operationId: judgeVerdictsProxyHandler
      tags:
        - extensions
      summary: GET /extensions/judge/verdicts.jsonl
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-1b40: operator-gated reverse proxy to the judge extension's /verdicts.jsonl export.
        Server-held base URL + export token from env (see ./judge-proxy.ts); never the SPA fallback.
  /forge/repos:
    get:
      operationId: forgeReposHandler
      tags:
        - forge
      summary: GET /forge/repos
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-2601: installation repo listing for the Add Project picker (readOperator — private
        repo names).
  /github-app/callback:
    get:
      operationId: gitHubAppCallbackHandler
      tags:
        - github-app
      summary: GET /github-app/callback
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /github-app/installed:
    get:
      operationId: gitHubAppInstalledHandler
      tags:
        - github-app
      summary: GET /github-app/installed
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: 'warren-54c7: the manifest setup_url target — GitHub''s post-install redirect.'
  /github-app/register:
    get:
      operationId: registerGitHubAppHandler
      tags:
        - github-app
      summary: GET /github-app/register
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: 'warren-a647: App manifest registration; anonymous — see ./github-app.ts.'
  /healthz:
    get:
      operationId: healthzHandler
      tags:
        - meta
      summary: GET /healthz
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /instance:
    get:
      operationId: instanceFactsHandler
      tags:
        - instance
      summary: GET /instance
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-2eec: read-only instance facts; spectators get the reduced projection
        (src/instance/facts.ts).
  /metrics:
    get:
      operationId: metricsHandler
      tags:
        - metrics
      summary: GET /metrics
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /ops/overview:
    get:
      operationId: opsOverviewHandler
      tags:
        - ops
      summary: GET /ops/overview
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        pl-7e38 step 12 (warren-d850): one-poll Operations snapshot, readPublic with a reduced
        projection.
  /plan-runs:
    get:
      operationId: listPlanRunsHandler
      tags:
        - plan-runs
      summary: GET /plan-runs
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  planRuns:
                    type: array
                    items:
                      $ref: '#/components/schemas/PlanRun'
                required:
                  - planRuns
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      operationId: createPlanRunHandler
      tags:
        - plan-runs
      summary: POST /plan-runs
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  planRun:
                    $ref: '#/components/schemas/PlanRun'
                  children:
                    type: array
                    items:
                      $ref: '#/components/schemas/PlanRunChild'
                required:
                  - planRun
                  - children
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /plan-runs/{id}:
    get:
      operationId: getPlanRunHandler
      tags:
        - plan-runs
      summary: GET /plan-runs/:id
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  planRun:
                    $ref: '#/components/schemas/PlanRun'
                  children:
                    type: array
                    items:
                      $ref: '#/components/schemas/PlanRunChild'
                  runs:
                    type: array
                    items:
                      $ref: '#/components/schemas/Run'
                required:
                  - planRun
                  - children
                  - runs
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plan-runs/{id}/cancel:
    post:
      operationId: cancelPlanRunHandler
      tags:
        - plan-runs
      summary: POST /plan-runs/:id/cancel
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  planRun:
                    $ref: '#/components/schemas/PlanRun'
                  cancelledChild:
                    type:
                      - object
                      - 'null'
                    properties:
                      childSeq:
                        type: integer
                      runId:
                        type: string
                  alreadyTerminal:
                    type: boolean
                required:
                  - planRun
                  - cancelledChild
                  - alreadyTerminal
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plan-runs/{id}/events:
    get:
      operationId: streamPlanRunEventsHandler
      tags:
        - plan-runs
      summary: GET /plan-runs/:id/events
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plan-runs/{id}/resume:
    post:
      operationId: resumePlanRunHandler
      tags:
        - plan-runs
      summary: POST /plan-runs/:id/resume
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /preview/config:
    get:
      operationId: previewConfigHandler
      tags:
        - preview
      summary: GET /preview/config
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /projects:
    get:
      operationId: listProjectsHandler
      tags:
        - projects
      summary: GET /projects
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  projects:
                    type: array
                    items:
                      $ref: '#/components/schemas/Project'
                required:
                  - projects
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      operationId: createProjectHandler
      tags:
        - projects
      summary: POST /projects
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  project:
                    $ref: '#/components/schemas/Project'
                required:
                  - project
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /projects/{id}:
    get:
      operationId: getProjectHandler
      tags:
        - projects
      summary: GET /projects/:id
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  project:
                    $ref: '#/components/schemas/Project'
                required:
                  - project
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
    delete:
      operationId: deleteProjectHandler
      tags:
        - projects
      summary: DELETE /projects/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/ready-plans:
    get:
      operationId: listReadyPlansHandler
      tags:
        - projects
      summary: GET /projects/:id/ready-plans
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/refresh:
    post:
      operationId: refreshProjectHandler
      tags:
        - projects
      summary: POST /projects/:id/refresh
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/seeds/plans:
    get:
      operationId: listProjectSeedPlansHandler
      tags:
        - projects
      summary: GET /projects/:id/seeds/plans
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        Static path — must precede `/projects/:id/seeds/:seedId` so the param route doesn't swallow
        `plans` as a seed id.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/seeds/{seedId}:
    get:
      operationId: getProjectSeedHandler
      tags:
        - projects
      summary: GET /projects/:id/seeds/:seedId
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: seedId
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/triggers:
    get:
      operationId: getProjectTriggersHandler
      tags:
        - projects
      summary: GET /projects/:id/triggers
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/triggers/{triggerId}/run:
    post:
      operationId: runProjectTriggerHandler
      tags:
        - projects
      summary: POST /projects/:id/triggers/:triggerId/run
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: triggerId
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/warren-config:
    get:
      operationId: getProjectWarrenConfigHandler
      tags:
        - projects
      summary: GET /projects/:id/warren-config
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /readyz:
    get:
      operationId: readyzHandler
      tags:
        - meta
      summary: GET /readyz
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /runs:
    get:
      operationId: listRunsHandler
      tags:
        - runs
      summary: GET /runs
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  runs:
                    type: array
                    items:
                      $ref: '#/components/schemas/Run'
                  total:
                    type: integer
                  limit:
                    type: integer
                  offset:
                    type: integer
                  costTotalUsd:
                    type:
                      - number
                      - 'null'
                  costPricedCount:
                    type: integer
                required:
                  - runs
                  - total
                  - limit
                  - offset
                  - costTotalUsd
                  - costPricedCount
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
    post:
      operationId: createRunHandler
      tags:
        - runs
      summary: POST /runs
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  run:
                    $ref: '#/components/schemas/Run'
                  sandbox:
                    type: object
                    properties:
                      id:
                        type: string
                      workspacePath:
                        type: string
                    required:
                      - id
                      - workspacePath
                required:
                  - run
                  - sandbox
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /runs/{id}:
    get:
      operationId: getRunHandler
      tags:
        - runs
      summary: GET /runs/:id
      responses:
        '200':
          description: Successful response.
          content:
            application/json:
              schema:
                type: object
                properties:
                  run:
                    $ref: '#/components/schemas/Run'
                required:
                  - run
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/cancel:
    post:
      operationId: cancelRunHandler
      tags:
        - runs
      summary: POST /runs/:id/cancel
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/events:
    get:
      operationId: streamRunEventsHandler
      tags:
        - runs
      summary: GET /runs/:id/events
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        NDJSON event tail. `?follow=1` live-tails (the default while the run is non-terminal);
        `?limit=N` requests a bounded non-streaming read of at most N events and implies
        follow=false — the response closes after the page, so agents can poll for liveness without
        holding a stream open (warren-17c1). `?since=<seq>` pages forward from a prior read.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/finalize-intent:
    get:
      operationId: getRunFinalizeIntentHandler
      tags:
        - runs
      summary: GET /runs/:id/finalize-intent
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-0d35: the in-pod finalize callback for the K8s backend — the pod fetches the reap
        intent, runs the workspace-dependent half in place, and POSTs the FinalizeResult back.
        Bearer-gated; the pod carries its per-run scoped token (warren-57fd).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/finalize-result:
    post:
      operationId: postRunFinalizeResultHandler
      tags:
        - runs
      summary: POST /runs/:id/finalize-result
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/git-credential:
    post:
      operationId: postRunGitCredentialHandler
      tags:
        - runs
      summary: POST /runs/:id/git-credential
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-c9ac: the in-pod credential re-mint (forge-contract.md §4.1 window 3) — the pod
        requests a freshly-minted push credential over the same authenticated callback channel
        instead of trusting the mounted Secret.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/inbox:
    get:
      operationId: pollRunInboxHandler
      tags:
        - runs
      summary: GET /runs/:id/inbox
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-3d0b: the in-pod steering poll for the K8s backend. Bearer-gated like every /runs
        route; the pod carries its per-run SCOPED token (warren-57fd). Destructive on read (it
        claims unread messages), so for a non-run caller it is operator-only (warren-b875).
        `?peek=1` lists the unread queue WITHOUT claiming (warren-3305) — the only safe way for an
        operator or the UI to inspect it; a bare poll steals the message from the pod's steering
        poll.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/preview/login:
    post:
      operationId: previewLoginHandler
      tags:
        - runs
      summary: POST /runs/:id/preview/login
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-e1b0: POST, not GET — the bearer rides the `Authorization` header like every other
        /runs route instead of a `?token=` query string that would land in history / Referer / proxy
        logs.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/preview/teardown:
    post:
      operationId: previewTeardownHandler
      tags:
        - runs
      summary: POST /runs/:id/preview/teardown
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/salvage:
    post:
      operationId: postRunSalvageHandler
      tags:
        - runs
      summary: POST /runs/:id/salvage
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-cd3b: the in-pod salvage intake — the pod POSTs the work it captured (rescue ref +
        git bundle) when the finalize branch push failed or no reap intent ever arrived, BEFORE its
        emptyDir dies with the pod.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/steer:
    post:
      operationId: steerRunHandler
      tags:
        - runs
      summary: POST /runs/:id/steer
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /setup:
    get:
      operationId: setupHandoffHandler
      tags:
        - setup
      summary: GET /setup
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: >-
        warren-48f8: setup-code redemption; anonymous by necessity — the code IS the auth. 404
        unless the boot armed the handoff (src/server/setup-handoff.ts).
  /version:
    get:
      operationId: versionHandler
      tags:
        - meta
      summary: GET /version
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
  /whoami:
    get:
      operationId: whoamiHandler
      tags:
        - whoami
      summary: GET /whoami
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
      description: 'warren-e195: `readPublic`, not `anonymous` — an exempt route gets no actor to name.'
components:
  schemas:
    RunState:
      type: string
      enum:
        - queued
        - running
        - succeeded
        - failed
        - cancelled
    RunFailureReason:
      type: string
      enum:
        - never_started
        - no_model_response
        - sandbox_failed
        - spawn_failed
        - crashed
        - agent_died
        - timed_out
        - sandbox_run_lost
        - sandbox_unreachable
        - dropped_commit
        - no_changes
        - finalize_failed
        - push_rejected_policy
        - finalize_unposted
        - provider_error
        - oom_killed
        - evicted
        - preempted
    RunMode:
      type: string
      enum:
        - batch
    RunCostBasis:
      type: string
      enum:
        - api
        - subscription_estimate
    CloneKind:
      type: string
      enum:
        - replicate
        - continue
    PreviewState:
      type: string
      enum:
        - starting
        - live
        - failed
        - torn-down
    EventStream:
      type: string
      enum:
        - stdout
        - stderr
        - system
    PlanRunState:
      type: string
      enum:
        - queued
        - running
        - succeeded
        - failed
        - cancelled
    PlanRunChildState:
      type: string
      enum:
        - pending
        - dispatched
        - running
        - pr_open
        - merged
        - failed
        - skipped
    AgentSource:
      type: string
      enum:
        - builtin
        - library
    PullRequestLifecycle:
      type: string
      enum:
        - open
        - merged
        - closed_unmerged
    ErrorEnvelope:
      type: object
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
            hint:
              type: string
          required:
            - code
            - message
      required:
        - error
    Project:
      type: object
      properties:
        id:
          type: string
        gitUrl:
          type: string
        localPath:
          type: string
        defaultBranch:
          type: string
        addedAt:
          type: string
        lastFetchedAt:
          type:
            - string
            - 'null'
        lastHeadSha:
          type:
            - string
            - 'null'
        hasSeeds:
          type: boolean
      required:
        - id
        - gitUrl
        - localPath
        - defaultBranch
        - addedAt
        - lastFetchedAt
        - lastHeadSha
        - hasSeeds
    Agent:
      type: object
      properties:
        name:
          type: string
        renderedJson: {}
        registeredAt:
          type: string
        lastRefreshed:
          type: string
        description:
          type:
            - string
            - 'null'
        provider:
          type:
            - string
            - 'null'
        model:
          type:
            - string
            - 'null'
        source:
          $ref: '#/components/schemas/AgentSource'
      required:
        - name
        - registeredAt
        - lastRefreshed
        - description
        - provider
        - model
    Run:
      type: object
      properties:
        id:
          type: string
        agentName:
          type: string
        projectId:
          type:
            - string
            - 'null'
        sandboxId:
          type:
            - string
            - 'null'
        sandboxRunId:
          type:
            - string
            - 'null'
        seedId:
          type:
            - string
            - 'null'
        parentRunId:
          type:
            - string
            - 'null'
        cloneKind:
          type:
            - string
            - 'null'
          enum:
            - replicate
            - continue
            - null
        mode:
          $ref: '#/components/schemas/RunMode'
        provider:
          type:
            - string
            - 'null'
        model:
          type:
            - string
            - 'null'
        renderedAgentJson: {}
        state:
          $ref: '#/components/schemas/RunState'
        failureReason:
          type:
            - string
            - 'null'
          enum:
            - never_started
            - no_model_response
            - sandbox_failed
            - spawn_failed
            - crashed
            - agent_died
            - timed_out
            - sandbox_run_lost
            - sandbox_unreachable
            - dropped_commit
            - no_changes
            - finalize_failed
            - push_rejected_policy
            - finalize_unposted
            - provider_error
            - oom_killed
            - evicted
            - preempted
            - null
        createdAt:
          type:
            - number
            - 'null'
        startedAt:
          type:
            - string
            - 'null'
        endedAt:
          type:
            - string
            - 'null'
        commitsAhead:
          type:
            - integer
            - 'null'
        filesChanged:
          type:
            - integer
            - 'null'
        insertions:
          type:
            - integer
            - 'null'
        deletions:
          type:
            - integer
            - 'null'
        prompt:
          type: string
        trigger:
          type: string
        prUrl:
          type:
            - string
            - 'null'
        prState:
          type:
            - string
            - 'null'
          enum:
            - open
            - merged
            - closed_unmerged
            - null
        prMergedAt:
          type:
            - string
            - 'null'
        targetBranch:
          type:
            - string
            - 'null'
        ref:
          type:
            - string
            - 'null'
        salvageRef:
          type:
            - string
            - 'null'
        salvagePath:
          type:
            - string
            - 'null'
        costUsd:
          type:
            - number
            - 'null'
        tokensInput:
          type:
            - integer
            - 'null'
        tokensOutput:
          type:
            - integer
            - 'null'
        tokensCacheRead:
          type:
            - integer
            - 'null'
        tokensCacheWrite:
          type:
            - integer
            - 'null'
        previewState:
          type:
            - string
            - 'null'
          enum:
            - starting
            - live
            - failed
            - torn-down
            - null
        previewPort:
          type:
            - integer
            - 'null'
        previewStartedAt:
          type:
            - string
            - 'null'
        previewLastHitAt:
          type:
            - string
            - 'null'
        previewFailureMessage:
          type:
            - string
            - 'null'
        costBasis:
          $ref: '#/components/schemas/RunCostBasis'
      required:
        - id
        - agentName
        - mode
        - state
        - prompt
        - trigger
        - costBasis
    PlanRun:
      type: object
      properties:
        id:
          type: string
        planId:
          type:
            - string
            - 'null'
        source:
          type: string
          enum:
            - plan
            - issues
        projectId:
          type: string
        agentName:
          type: string
        promptTemplate:
          type: string
        ref:
          type:
            - string
            - 'null'
        providerOverride:
          type:
            - string
            - 'null'
        modelOverride:
          type:
            - string
            - 'null'
        maxCostUsd:
          type:
            - number
            - 'null'
        dispatcherHandle:
          type: string
        trigger:
          type: string
        state:
          $ref: '#/components/schemas/PlanRunState'
        failureReason:
          type:
            - string
            - 'null'
        createdAt:
          type: string
        startedAt:
          type:
            - string
            - 'null'
        endedAt:
          type:
            - string
            - 'null'
      required:
        - id
        - source
        - projectId
        - agentName
        - promptTemplate
        - dispatcherHandle
        - trigger
        - state
        - createdAt
    PlanRunChild:
      type: object
      properties:
        planRunId:
          type: string
        seq:
          type: integer
        seedId:
          type: string
        runId:
          type:
            - string
            - 'null'
        state:
          $ref: '#/components/schemas/PlanRunChildState'
        createdAt:
          type: string
        updatedAt:
          type: string
        startedAt:
          type:
            - string
            - 'null'
        endedAt:
          type:
            - string
            - 'null'
        prMergedAt:
          type:
            - string
            - 'null'
        failureReason:
          type:
            - string
            - 'null'
      required:
        - planRunId
        - seq
        - seedId
        - runId
        - state
        - createdAt
        - updatedAt
