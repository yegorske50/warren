# AUTO-GENERATED by `bun run gen:openapi` from `src/server/handlers/index.ts`.
# Do not edit by hand. CI fails if this file is out of sync.
openapi: 3.1.0
info:
  title: warren HTTP API
  version: 0.7.11
  description: >-
    Auto-generated from `src/server/handlers/index.ts`'s `ROUTE_TABLE`. Run `bun run gen:openapi` to
    refresh; CI fails if this schema drifts from the handler module. Request/response bodies are
    intentionally left permissive in V1 — see SPEC §8.1 for the canonical handler contracts.
tags:
  - name: agents
    description: '`/agents` routes.'
  - name: analytics
    description: '`/analytics` routes.'
  - name: brainstorm
    description: '`/brainstorm` routes.'
  - name: burrows
    description: '`/burrows` routes.'
  - name: meta
    description: Liveness, readiness, and version.
  - name: plan-runs
    description: '`/plan-runs` routes.'
  - name: plot-plan-runs
    description: '`/plot-plan-runs` routes.'
  - name: plots
    description: '`/plots` routes.'
  - name: preview
    description: '`/preview` routes.'
  - name: projects
    description: '`/projects` routes.'
  - name: runs
    description: '`/runs` routes.'
  - name: workers
    description: '`/workers` routes.'
paths:
  /agents:
    get:
      operationId: listAgentsHandler
      tags:
        - agents
      summary: GET /agents
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /agents/refresh:
    post:
      operationId: refreshAgentsHandler
      tags:
        - agents
      summary: POST /agents/refresh
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /agents/{name}:
    get:
      operationId: getAgentHandler
      tags:
        - agents
      summary: GET /agents/:name
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
  /analytics/behavior:
    get:
      operationId: listBehaviorAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/behavior
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /analytics/cost:
    get:
      operationId: listCostAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/cost
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /analytics/runs:
    get:
      operationId: listRunAnalyticsHandler
      tags:
        - analytics
      summary: GET /analytics/runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /brainstorm:
    post:
      operationId: createBrainstormHandler
      tags:
        - brainstorm
      summary: POST /brainstorm
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /burrows:
    get:
      operationId: listBurrowsHandler
      tags:
        - burrows
      summary: GET /burrows
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /burrows/{id}:
    get:
      operationId: getBurrowHandler
      tags:
        - burrows
      summary: GET /burrows/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /healthz:
    get:
      operationId: healthzHandler
      tags:
        - meta
      summary: GET /healthz
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /plan-runs:
    get:
      operationId: listPlanRunsHandler
      tags:
        - plan-runs
      summary: GET /plan-runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
    post:
      operationId: createPlanRunHandler
      tags:
        - plan-runs
      summary: POST /plan-runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /plan-runs/{id}:
    get:
      operationId: getPlanRunHandler
      tags:
        - plan-runs
      summary: GET /plan-runs/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plan-runs/{id}/cancel:
    post:
      operationId: cancelPlanRunHandler
      tags:
        - plan-runs
      summary: POST /plan-runs/:id/cancel
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plan-runs/{id}/events:
    get:
      operationId: streamPlanRunEventsHandler
      tags:
        - plan-runs
      summary: GET /plan-runs/:id/events
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plot-plan-runs:
    post:
      operationId: createPlotPlanRunHandler
      tags:
        - plot-plan-runs
      summary: POST /plot-plan-runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /plots:
    get:
      operationId: listPlotsHandler
      tags:
        - plots
      summary: GET /plots
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
    post:
      operationId: createPlotHandler
      tags:
        - plots
      summary: POST /plots
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /plots/needs-attention/count:
    get:
      operationId: needsAttentionCountHandler
      tags:
        - plots
      summary: GET /plots/needs-attention/count
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        Static path — must precede `/plots/:id` so the param route doesn't swallow `needs-attention`
        as an :id.
  /plots/{id}:
    get:
      operationId: getPlotHandler
      tags:
        - plots
      summary: GET /plots/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/attachments:
    post:
      operationId: attachPlotHandler
      tags:
        - plots
      summary: POST /plots/:id/attachments
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/attachments/{ref}:
    delete:
      operationId: detachPlotHandler
      tags:
        - plots
      summary: DELETE /plots/:id/attachments/:ref
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: ref
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/attachments/{ref}/merge:
    post:
      operationId: mergePlotPrAttachmentHandler
      tags:
        - plots
      summary: POST /plots/:id/attachments/:ref/merge
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        Specific path — must precede `/plots/:id/attachments/:ref` so the DELETE-by-ref route
        doesn't swallow `<ref>/merge` as a ref.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: ref
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/formalize:
    post:
      operationId: formalizePlotHandler
      tags:
        - plots
      summary: POST /plots/:id/formalize
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/intent:
    post:
      operationId: editPlotIntentHandler
      tags:
        - plots
      summary: POST /plots/:id/intent
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/questions/{event_id}/answer:
    post:
      operationId: answerPlotQuestionHandler
      tags:
        - plots
      summary: POST /plots/:id/questions/:event_id/answer
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: event_id
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/rename:
    post:
      operationId: renamePlotHandler
      tags:
        - plots
      summary: POST /plots/:id/rename
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/status:
    post:
      operationId: changePlotStatusHandler
      tags:
        - plots
      summary: POST /plots/:id/status
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/summary:
    get:
      operationId: getPlotSummaryHandler
      tags:
        - plots
      summary: GET /plots/:id/summary
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      description: >-
        Static-suffix path — must precede `/plots/:id` so the param route doesn't swallow `summary`
        as the rest of the id.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /plots/{id}/sync:
    post:
      operationId: syncPlotHandler
      tags:
        - plots
      summary: POST /plots/:id/sync
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /preview/config:
    get:
      operationId: previewConfigHandler
      tags:
        - preview
      summary: GET /preview/config
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /projects:
    get:
      operationId: listProjectsHandler
      tags:
        - projects
      summary: GET /projects
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
    post:
      operationId: createProjectHandler
      tags:
        - projects
      summary: POST /projects
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /projects/{id}:
    delete:
      operationId: deleteProjectHandler
      tags:
        - projects
      summary: DELETE /projects/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/agents/refresh:
    post:
      operationId: refreshProjectAgentsHandler
      tags:
        - projects
      summary: POST /projects/:id/agents/refresh
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/refresh:
    post:
      operationId: refreshProjectHandler
      tags:
        - projects
      summary: POST /projects/:id/refresh
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/seeds/{seedId}:
    get:
      operationId: getProjectSeedHandler
      tags:
        - projects
      summary: GET /projects/:id/seeds/:seedId
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: seedId
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/triggers:
    get:
      operationId: getProjectTriggersHandler
      tags:
        - projects
      summary: GET /projects/:id/triggers
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/triggers/{triggerId}/run:
    post:
      operationId: runProjectTriggerHandler
      tags:
        - projects
      summary: POST /projects/:id/triggers/:triggerId/run
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: triggerId
          in: path
          required: true
          schema:
            type: string
  /projects/{id}/warren-config:
    get:
      operationId: getProjectWarrenConfigHandler
      tags:
        - projects
      summary: GET /projects/:id/warren-config
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /readyz:
    get:
      operationId: readyzHandler
      tags:
        - meta
      summary: GET /readyz
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /runs:
    get:
      operationId: listRunsHandler
      tags:
        - runs
      summary: GET /runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
    post:
      operationId: createRunHandler
      tags:
        - runs
      summary: POST /runs
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /runs/{id}:
    get:
      operationId: getRunHandler
      tags:
        - runs
      summary: GET /runs/:id
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/cancel:
    post:
      operationId: cancelRunHandler
      tags:
        - runs
      summary: POST /runs/:id/cancel
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/events:
    get:
      operationId: streamRunEventsHandler
      tags:
        - runs
      summary: GET /runs/:id/events
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/messages:
    post:
      operationId: postRunMessageHandler
      tags:
        - runs
      summary: POST /runs/:id/messages
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/preview/login:
    get:
      operationId: previewLoginHandler
      tags:
        - runs
      summary: GET /runs/:id/preview/login
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/preview/teardown:
    post:
      operationId: previewTeardownHandler
      tags:
        - runs
      summary: POST /runs/:id/preview/teardown
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /runs/{id}/steer:
    post:
      operationId: steerRunHandler
      tags:
        - runs
      summary: POST /runs/:id/steer
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
  /version:
    get:
      operationId: versionHandler
      tags:
        - meta
      summary: GET /version
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /workers:
    get:
      operationId: listWorkersHandler
      tags:
        - workers
      summary: GET /workers
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
  /workers/{name}/drain:
    post:
      operationId: drainWorkerHandler
      tags:
        - workers
      summary: POST /workers/:name/drain
      responses:
        '200':
          description: Successful response.
        default:
          description: Error response (see `src/core/errors.ts`).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
