{"type":"convention","classification":"tactical","recorded_at":"2026-05-08T20:48:07.956Z","evidence":{"commit":"e710a98a2ca955cb6635288e44e3491a8ae9b9e0"},"content":"src/projects/ module mirrors src/registry/'s file layout: errors.ts, config.ts (env vars), url.ts (parser), clone.ts (git shell-out), manage.ts (high-level operations), index.ts (re-exports), plus *.test.ts. Each high-level entry takes injectable spawn + filesystem stubs so tests don't shell out or touch disk.","id":"mx-3443cd"}
{"type":"decision","classification":"tactical","recorded_at":"2026-05-08T20:48:15.415Z","evidence":{"commit":"e710a98a2ca955cb6635288e44e3491a8ae9b9e0"},"title":"Project clones are working trees, not caches","rationale":"Unlike the canopy library clone (cached, hard-reset to origin/HEAD on every refresh), project clones are working trees that agents will actively run against. So src/projects/clone.ts deliberately does *not* support fast-forwarding an existing dir — it refuses with ProjectUnavailableError if the target path exists. Re-adding a project means explicit DELETE + re-clone, so the operator owns destructive resets.","id":"mx-fe890c"}
{"type":"decision","classification":"tactical","recorded_at":"2026-05-08T20:48:18.658Z","evidence":{"commit":"e710a98a2ca955cb6635288e44e3491a8ae9b9e0"},"title":"deleteProject removes disk before row; addProject inserts row only after clone succeeds","rationale":"Maintains the invariant 'a projects row exists ⇒ its localPath exists on disk'. addProject: validation/duplicate checks short-circuit before any disk work; clone failure rolls back any partial dir; row inserts only after clone returns success. deleteProject: rm-rf first, then repo.delete; if rm fails the row stays registered so the operator sees the failure and can retry, rather than ending up with a ghost row pointing at half a directory.","id":"mx-be7a74"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-05-08T20:48:22.160Z","evidence":{"commit":"e710a98a2ca955cb6635288e44e3491a8ae9b9e0"},"name":"assertPathUnderRoot guard before rm-rf","description":"deleteProject calls assertPathUnderRoot(row.localPath, config.root) before any rmrf. Uses path.resolve on both sides + startsWith(root + sep) check. Defense-in-depth: a tampered or stale db row whose localPath escaped /data/projects (e.g. '/etc/passwd') throws ProjectUnavailableError instead of letting warren rm-rf an arbitrary path. Test 'refuses to delete a project whose localPath escaped the configured root' enforces this.","files":[".mulch/mulch.config.yaml",".seeds/issues.jsonl"],"id":"mx-958c42"}
{"type":"convention","classification":"tactical","recorded_at":"2026-05-08T20:48:25.998Z","evidence":{"commit":"e710a98a2ca955cb6635288e44e3491a8ae9b9e0"},"content":"GitHub URL parser (src/projects/url.ts) accepts only github.com hosts in three shapes: https://github.com/<owner>/<name>[.git], git@github.com:<owner>/<name>[.git], ssh://git@github.com/<owner>/<name>[.git]. Other hosts (gitlab, self-hosted) are rejected with ValidationError per SPEC §3.1's 'paste a GitHub URL' scope. Owner/name segments must match [A-Za-z0-9._-]+, may not be '.', '..', or start with '-' — keeps /data/projects/<owner>/<name> path safe.","id":"mx-e741b0"}
{"type":"convention","classification":"tactical","recorded_at":"2026-05-08T20:48:28.153Z","evidence":{"commit":"e710a98a2ca955cb6635288e44e3491a8ae9b9e0"},"content":"Project management env vars: WARREN_PROJECTS_DIR (root for cloned project repos, default /data/projects) and WARREN_GIT_BINARY (default 'git', shared with the canopy registry — both modules read the same var so a single git binary override applies everywhere).","id":"mx-6fe065"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-05-09T19:29:07.947Z","evidence":{"seeds":"warren-1bb6","commit":"43e589bf23f1438b472c264832e3e54c92910839"},"dir_anchors":["src/projects"],"name":"refresh-on-run","description":"Project clones must be refreshed (git fetch + checkout + reset --hard origin/<ref>) before every spawnRun, otherwise /data/projects/<id>/ stays frozen at registration-time forever and agents work on stale code (warren-1bb6). The refresh runs BEFORE the warren run row is created so a fetch failure aborts the spawn cleanly without stranding a queued row. The default ref is the project's tracked default_branch; callers can override via POST /runs body.ref to point a run at a specific branch/tag/SHA. The same refreshProject() call backs the explicit POST /projects/:id/refresh endpoint.","files":[".seeds/issues.jsonl","scripts/acceptance/lib/assert.ts","scripts/acceptance/run.ts","scripts/acceptance/scenarios/03-projects-management.ts","src/db/migrations/meta/_journal.json","src/db/repos/projects.ts","src/db/schema.ts","src/projects/index.ts","src/projects/manage.test.ts","src/projects/manage.ts","src/runs/spawn.test.ts","src/runs/spawn.ts","src/server/handlers.test.ts","src/server/handlers.ts","src/ui/src/api/client.ts","src/ui/src/api/types.ts","src/ui/src/pages/Projects.tsx"],"id":"mx-a3a4ad"}
{"type":"decision","classification":"tactical","recorded_at":"2026-05-09T19:29:17.775Z","evidence":{"seeds":"warren-1bb6","commit":"43e589bf23f1438b472c264832e3e54c92910839"},"dir_anchors":["src/projects"],"title":"Project clones are working trees that get re-fetched, not caches that get re-cloned","rationale":"Operators register one project per repo and reuse it across many runs (per warren-1bb6 user feedback: 'I'm not going to create a new project for every PR/issue I work on, that defeats the whole purpose'). The lifecycle is: POST /projects = first-time clone, POST /projects/:id/refresh = git fetch + reset --hard origin/<ref>, DELETE = rm-rf + drop row. POST /runs implicitly refreshes before each dispatch. New columns last_fetched_at + last_head_sha let the UI surface 'is this clone current?' without re-shelling-out to git on every list. Hard-reset semantics (not pull/merge) match canopy's clone — warren never preserves agent-side commits across runs; that's burrow's job via the workspace branch.","id":"mx-791a7a"}
{"type":"convention","classification":"tactical","recorded_at":"2026-05-09T19:29:37.562Z","evidence":{"seeds":"warren-1bb6","commit":"43e589bf23f1438b472c264832e3e54c92910839"},"dir_anchors":["src/projects"],"content":"refreshProjectClone runs git fetch --prune origin → git checkout --force <ref> → git reset --hard origin/<ref>, with a fallback to plain 'git reset --hard <ref>' when origin/<ref> doesn't resolve (so SHAs and tags work, not just remote branch names). HEAD sha is read via 'git rev-parse HEAD' after reset. Spawn + existsSync injection lets tests stub git without disk. ProjectUnavailableError is raised when the localPath has vanished (data-integrity issue, not a recoverable refresh).","id":"mx-6e85b9"}
{"type":"decision","classification":"tactical","recorded_at":"2026-05-09T21:33:19.657Z","evidence":{"seeds":"warren-5f19","commit":"77c006655d9b863c408e8f948c66378fce700341"},"relates_to":["mx-be7a74"],"title":"deleteProject deletes row first, then disk; runs orphan via ON DELETE SET NULL","rationale":"The original ordering (rmrf disk → repo.delete row, mx-be7a74) maintained the invariant 'a projects row exists ⇒ its localPath exists on disk', but combined with the FK shape on runs.project_id (NOT NULL, no onDelete clause) it state-corrupted on every delete of a project that had runs: SQLite raised a FOREIGN KEY constraint failed AFTER rmrf had wiped the disk, leaving the row pointing at a missing directory. Subsequent dispatches against that project then hard-failed with 'project clone missing on disk'. The fix changes the FK to ON DELETE SET NULL (so runs orphan instead of blocking the delete) and inverts the ordering: row delete first, best-effort disk rmrf second. The new failure mode is 'no row, stranded disk under WARREN_PROJECTS_DIR' — recoverable (operator can re-register the same gitUrl, or rm the dir manually) and never wedges the system. The on-disk orphan is logged via input.logger.warn instead of thrown; the row is the meaningful deliverable so the HTTP 200 still returns it.","id":"mx-30e91a"}
{"type":"convention","classification":"tactical","recorded_at":"2026-05-13T22:54:23.614Z","evidence":{"seeds":"warren-9f70","commit":"12e64323189fbf63dccfa921db204ebbb65f3951"},"content":"refreshProjectClone (warren-9f70) runs 'git config --local --unset-all user.name' then '--unset-all user.email' after the hard-reset on every dispatch. Both are best-effort: --unset-all exits 5 ('no such key') when absent, the normal case for clean clones, so trySpawn (not runGit) is used so a non-zero exit does not abort the refresh. Companion change: the acceptance harness no longer writes [user] anywhere — fixtures.ts writeGitConfigRedirects and inproc.ts fallback writeFile dropped their [user] blocks; identity comes only from GIT_AUTHOR_*/GIT_COMMITTER_* env vars. scripts/acceptance/run.ts runInProcMode adds a post-run guardrail (collectUserIdentityLeaks + findUserKeys) that fails the harness if any project clone ends a run with user.name or user.email set in its local .git/config.","id":"mx-8be13e"}
{"type":"pattern","classification":"tactical","recorded_at":"2026-05-17T22:03:54.103Z","evidence":{"commit":"8c1c44d0fd1c7c24ec2ed87fb88ffb10f36e1973"},"dir_anchors":["src/projects"],"name":"feature-detection-on-refresh","description":"Project opt-in integration detection lives in src/projects/refresh.ts as detectProjectFeatures(localPath, exists?). refreshProjectClone calls it after the post-fetch hard-reset and returns the booleans on RefreshProjectCloneResult.features; addProject runs the same probe right after cloneProjectRepo so the row reflects the freshly-checked-out tree from the first request. ProjectsRepo.create accepts a hasPlot input; recordRefresh accepts an optional hasPlot patch (omitted means leave prior). The UI wire type ProjectRow mirrors the new boolean column. Tests inject detectFeatures / spawn / exists rather than touching disk. PROJECT_FEATURE_DIRS centralizes the directory names so adding .mulch/.seeds/.canopy/.pi/ in the same shape later is a one-line extension.","files":[".seeds/issues.jsonl","src/db/migrations/meta/_journal.json","src/db/migrations/postgres/meta/_journal.json","src/db/repos/projects.ts","src/db/schema/postgres.ts","src/db/schema/sqlite.ts","src/projects/index.ts","src/projects/manage.test.ts","src/projects/manage.ts","src/projects/refresh.test.ts","src/projects/refresh.ts","src/ui/src/api/types.ts"],"id":"mx-62ef33"}
